const initialState = {
   smurfs: []
};

const smurfReducer = (state = initialState, { type, payload }) => {
   switch (type) {
      default:
         state = { ...state };
   }
};

export default smurfReducer;
